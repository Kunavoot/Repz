# 🚀 ตัวอย่าง Prompt สำหรับ Development (Development Prompts Guide)

เอกสารนี้รวบรวมตัวอย่าง Prompt ที่ใช้บ่อยในการพัฒนาซอฟต์แวร์ โดยแบ่งเป็น 2 ประเภทหลัก คือ **แบบมีประสิทธิภาพ (Highly Effective)** สำหรับงานที่ต้องการความแม่นยำสูง และ **แบบประจำวัน (Daily Use)** สำหรับงานทั่วไปที่เน้นความรวดเร็ว

---

## 🌟 1. แบบมีประสิทธิภาพ (Highly Effective Prompts)
เหมาะสำหรับงานที่ซับซ้อน ต้องการบริบทชัดเจน ลดข้อผิดพลาด และต้องการผลลัพธ์ที่นำไปใช้ได้ทันที มักจะประกอบด้วย Role, Context, Task, Constraints และ Format

### 🛠️ 1.1 การสร้างฟีเจอร์ใหม่ (Feature Implementation)
```text
ในฐานะ Senior React Developer, ฉันต้องการให้คุณเขียนคอมโพเนนต์ `UserProfile` สำหรับแอปพลิเคชัน Next.js (App Router)

Context: ระบบใช้ Tailwind CSS สำหรับจัดการสไตล์ และใช้ TypeScript สำหรับ Type Checking ข้อมูล User ถูกดึงมาจาก API `/api/user`

Requirements:
- ต้องมี State สำหรับ Loading และ Error handling
- สร้าง Skeleton loading ระหว่างรอข้อมูล
- ดึงข้อมูลโดยใช้ `useQuery` จาก React Query
- รองรับ Responsive design (Mobile & Desktop)

Output: ขอโค้ดที่พร้อมใช้งาน แบ่งไฟล์แยกถ้าจำเป็น และใส่คอมเมนต์อธิบายการทำงานที่สำคัญ
```

### 🐛 1.2 การแก้ไขบั๊ก (Bug Fixing & Debugging)
```text
ฉันเจอบั๊กในฟังก์ชันคำนวณราคาสินค้า (`calculateTotal`) ในไฟล์ `cart.ts`

อาการที่พบ: เมื่อใส่สินค้าที่มีส่วนลด 50% ระบบคำนวณราคาสุทธิผิดพลาด (ได้ค่าติดลบในบางกรณี)

โค้ดปัจจุบัน:
[วางโค้ดปัจจุบันของคุณที่นี่]

สิ่งที่คุณต้องทำ:
1. วิเคราะห์หาสาเหตุที่ทำให้เกิดบั๊กนี้
2. เสนอวิธีการแก้ไขที่ถูกต้องที่สุด พร้อมอธิบายว่าทำไมถึงแก้แบบนี้
3. เขียน Unit Test (ใช้ Jest) ครอบคลุมกรณี Edge case นี้เพื่อป้องกันบั๊กกลับมา
```

### ♻️ 1.3 การรีแฟกเตอร์โค้ด (Code Refactoring)
```text
ช่วย Refactor โค้ดฟังก์ชัน `processOrder` ด้านล่างนี้ ให้เป็นไปตามหลัก Clean Code และ SOLID Principles

เป้าหมาย:
- ลดความซับซ้อน (Cyclomatic complexity) โดยแยกการทำงานเป็นฟังก์ชันย่อย
- กำจัด Code Smell หรือตัวแปรที่ไม่ได้ใช้
- เพิ่ม Type Definition ให้รัดกุมขึ้น (TypeScript)
- ห้ามเปลี่ยน Business Logic เดิมเด็ดขาด

โค้ด:
[วางโค้ดที่ต้องการ Refactor]
```

### 👁️ 1.4 การรีวิวโค้ด (Code Review)
```text
ในฐานะ Senior Staff Engineer, โปรดช่วยรีวิวโค้ดนี้ (Pull Request) ให้หน่อย

เป้าหมายการรีวิว:
- หาข้อผิดพลาดของ Logic และบั๊กแฝง (Edge cases, Race conditions)
- หาจุดที่อาจเกิดปัญหาด้าน Performance หรือ Security
- ตรวจสอบว่าโค้ดทำตาม Best Practices หรือไม่

สิ่งที่ฉันคาดหวัง:
- ชี้ให้เห็นถึงจุดที่ควรแก้ไขอย่างเจาะจง (ระบุบรรทัดถ้าเป็นไปได้)
- เสนอโค้ดทางเลือก (Alternative implementation) ที่ดีกว่า พร้อมอธิบายเหตุผล

โค้ดที่ต้องการให้รีวิว:
[วางโค้ดที่ต้องการรีวิว หรือ Git Diff]
```

### 🏗️ 1.5 การออกแบบระบบและโครงสร้าง (System Design & Architecture)
```text
ในฐานะ Software Architect, ฉันต้องการให้คุณช่วยออกแบบ Database Schema สำหรับระบบ E-commerce ขนาดกลาง

Context: ใช้ PostgreSQL เป็นฐานข้อมูลหลัก เน้นการอ่านข้อมูล (Read-heavy) และต้องรองรับระบบตะกร้าสินค้า คูปองส่วนลด และประวัติการสั่งซื้อ

สิ่งที่ต้องการ:
- โครงสร้างตาราง (Tables) พร้อม Data types
- ระบุ Primary Key, Foreign Key และ Indexes ที่จำเป็น
- แนะนำแนวทางการจัดการสต็อกสินค้า (Inventory) เพื่อป้องกันปัญหา Race Condition ตอนคนซื้อพร้อมกัน
- แสดงตัวอย่างการ Join ข้อมูลที่ใช้งานบ่อย (เช่น ดึง Order history ของ User)
```

### 🧠 1.6 การระดมสมองและเปรียบเทียบทางเลือก (Brainstorming & Trade-offs)
```text
โปรเจกต์ของฉันต้องการทำระบบ Background Jobs สำหรับส่งอีเมลแจ้งเตือนผู้ใช้งาน

บริบท:
- เซิร์ฟเวอร์ปัจจุบันเป็น Node.js รันบน Docker (มี Resource แรมจำกัด)
- ปริมาณอีเมลประมาณ 5,000 ฉบับต่อวัน แต่จะมีช่วงพีคตอนสิ้นเดือน
- ทีมมีความรู้พื้นฐานแค่ Redis ยังไม่เคยใช้ RabbitMQ หรือ Kafka

คำถาม:
- ระหว่าง BullMQ (บน Redis) กับ RabbitMQ ฉันควรเลือกตัวไหนดี?
- โปรดเปรียบเทียบข้อดี ข้อเสีย ความซับซ้อนในการดูแลรักษา และความเหมาะสมกับบริบทของฉันให้เป็นตาราง
```

---

## ⚡ 2. แบบประจำวัน (Daily Use Prompts)
เหมาะสำหรับงานสั้นๆ ถาม-ตอบ ค้นหาวิธีแก้ปัญหาด่วน หรือขอตัวอย่างโค้ดแบบรวดเร็ว

### 📖 2.1 ถามวิธีทำหรือขอโค้ดสั้นๆ (Quick How-to & Snippets)
```text
ช่วยเขียน Regex สำหรับตรวจสอบเบอร์โทรศัพท์ประเทศไทย (08x, 09x, 06x) ให้หน่อย
```

```text
วิธีใช้ `reduce` ใน JavaScript เพื่อจัดกลุ่ม Array of Objects ตาม key `category` พร้อมตัวอย่างโค้ด
```

```text
คำสั่ง Docker สำหรับรัน PostgreSQL 15 พร้อมเซ็ต Username, Password และ Database name ในบรรทัดเดียว
```

```text
ช่วยแปลง JSON นี้เป็น TypeScript Interface ให้ที: [วาง JSON]
```

### 🔍 2.2 อธิบายโค้ด (Explain Code)
```text
โค้ดบรรทัดนี้ทำงานยังไง อธิบายให้เด็กจบใหม่ฟังหน่อย: `const unique = [...new Set(arr)];`
```

```text
ฟังก์ชันนี้มี Big O Notation (Time/Space Complexity) เป็นเท่าไหร่ และจะปรับให้เร็วขึ้นได้ยังไง? [วางโค้ด]
```

```text
ช่วยสรุปสั้นๆ ว่า React `useMemo` ต่างจาก `useCallback` ยังไง และควรใช้ตอนไหน
```

### 🧪 2.3 ช่วยเขียนเทสต์ (Quick Testing)
```text
ช่วยเขียน Unit test พื้นฐานด้วย Vitest สำหรับฟังก์ชัน `isEmailValid(email: string): boolean` ให้หน่อย เอาแค่ Success case 2 อัน และ Fail case 2 อัน
```

### 📝 2.4 เขียนเอกสารและคอมเมนต์ (Docs & Comments)
```text
ช่วยเขียน JSDoc สำหรับฟังก์ชันนี้ให้ที [วางโค้ด]
```

```text
ช่วยร่าง README.md สั้นๆ สำหรับโปรเจกต์นี้ให้หน่อย มีแค่หัวข้อ: วิธีติดตั้ง, วิธีรัน, และวิธีเทสต์
```

```text
ตั้งชื่อตัวแปร boolean สำหรับเช็คว่า 'กำลังอัปโหลดไฟล์อยู่' ให้ดูเป็นมืออาชีพหน่อย ขอมา 5 ตัวเลือก
```

### 🚨 2.5 การวิเคราะห์ Error Log (Error & Stack Trace Analysis)
```text
ทำไมรันคำสั่งนี้แล้วพัง?
Environment: Node 18, Windows
Error Log:
[วาง Error Log ทั้งหมดตรงนี้]
```

```text
เกิด Timeout ตอน Query ข้อมูล: [วาง Error Message]
ตารางฉันมีข้อมูล 1 ล้าน record เกิดจาก Index หายไปหรือเปล่า? ช่วยวิเคราะห์และเสนอท่าแก้เบื้องต้นที
```
